# Market data

Place user-supplied market CSV files here if you add a manual import workflow. Do not commit licensed vendor data.

The current live configuration uses `yfinance` for SPY, XLF, XLE, WTI, USD/CAD, and USD/KRW. No API key is required.

