# Macro data

Place manually sourced macro CSV files here if you extend the loader. Do not commit licensed vendor data.

The current live configuration uses the official FRED API for `DGS10`, `DFF`, and `CPIAUCSL`. See `.env.example` for the blank API-key field.

